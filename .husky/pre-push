#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Full TypeScript type check before pushing. Kept out of pre-commit to keep
# commits fast; CI remains the authoritative gate.
# Mesmo comando do gate de CI (npm run check = tsc), para manter alinhamento.
echo "Running TypeScript type check (npm run check)..."
npm run check
